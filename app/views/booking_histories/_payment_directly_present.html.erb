<% if order.find_payment_directly.present? %>
  <div class="col-md-4">
    <div class="widget display-widget">
      <div class="widget-extra-full text-center"><%= t "booking_histories.choose_payment.directly"%><br/>
        <%= link_to image_tag("payment_methods/directly_logo.png", class: "themed-background-fixed2"),
          "#", class: "payment-directly", checked: false, id: "#{order.id}" %>
      </div>
    </div>
  </div>
<% end %>
